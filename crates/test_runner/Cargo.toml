[package]
name = "test_runner"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
reqwest = { version = "0.11", default-features = false, features = [
    "rustls-tls",
    "blocking",
] }
